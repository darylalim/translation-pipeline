{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */uv.lock|uv.lock|*/.env|.env|*/.streamlit/secrets.toml|.streamlit/secrets.toml) printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Edits to uv.lock, .env, and .streamlit/secrets.toml are blocked: change uv.lock via uv (uv add / uv lock / uv sync), and edit the gitignored secret files by hand.\"}}' ;; esac; }",
            "statusMessage": "Checking protected paths..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [ -n \"$f\" ] || exit 0; case \"$f\" in *.py) uv run ruff format \"$f\" && uv run ruff check --fix \"$f\" ;; esac; } || true",
            "statusMessage": "Formatting with ruff..."
          }
        ]
      }
    ]
  }
}
